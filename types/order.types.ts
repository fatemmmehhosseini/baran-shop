
export type Address = {
 fullName: string;
  phone: string;
  province: string;
  city: string;
  address: string;
  postalCode: string;
};

export type CartItem = {
  productId: number;
  quantity: number;
  color: string;
  size: string;
};